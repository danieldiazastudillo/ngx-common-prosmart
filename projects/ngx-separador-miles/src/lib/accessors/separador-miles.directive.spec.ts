import { SeparadorMilesDirective } from './separador-miles.directive';

describe('SeparadorMilesDirective', () => {
  it('should create an instance', () => {
    const directive = new SeparadorMilesDirective();
    expect(directive).toBeTruthy();
  });
});
