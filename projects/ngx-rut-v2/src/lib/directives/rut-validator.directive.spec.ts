import { RutValidator } from './rut-validator.directive';

describe('RutValidator', () => {
  it('should create an instance', () => {
    const directive = new RutValidator();
    expect(directive).toBeTruthy();
  });
});
